export declare abstract class CustomError extends Error {
    constructor(message: string);
    abstract statusCode: number;
    abstract seralizeErrors(): {
        message: string;
        field?: string;
    }[];
}
